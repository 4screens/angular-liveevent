module Page {
  export enum CaseType {
    Undefined,
    Image,
    Input,
    Iteration,
    Text
  }

  export enum Type {
    Undefined,
    EndPage,
    Form,
    MultiChoice,
    PictureChoice,
    Rateit,
    StartPage,
    Buzzer,
    Poster
  }
}
